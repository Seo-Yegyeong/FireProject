import 'package:flutter/material.dart';

class AnnounceUpdatePage extends StatelessWidget {
  const AnnounceUpdatePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
